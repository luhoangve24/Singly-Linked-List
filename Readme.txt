- Giải nén ra và chọn file main.cpp để chạy
- File LinkedList.h và file LinkedList2.h là những file nói nôm na là tự dựng thư viện, 
có thể mở ra xem hoặc không cần cũng được
- Khi chạy chương trình, có hiện ra Menu thì đọc các tùy chọn và sử dụng
- Bản chất của Danh sách liên kết là không cần đưa ra số ô như trong 1 mảng ( VD : Bình thường
khi khai báo mảng thì phải khai báo chiều dài (số ô) là n = x, nhưng danh sách liên kết đơn thì
không cần) nên muốn thêm thì chọn 0 ( thêm trước hay sau gì đó mình không nhớ ) hoặc 1 để thêm,
nhập số mình cần thêm vào. Hoặc chọn tính năng "... create random Singly Linked List" để tạo 1 
mảng ngẫu nhiên với n = x cho trước.